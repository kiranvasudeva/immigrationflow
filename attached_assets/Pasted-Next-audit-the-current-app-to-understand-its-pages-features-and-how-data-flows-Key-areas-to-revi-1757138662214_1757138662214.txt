Next, audit the current app to understand its pages, features, and how data flows. Key areas to review:
Clients Page & Model: The Clients page lists client organizations (companies). Each client has a profile with fields like legal name, registration number, CUI (tax ID), address, etc. In the database, the client_profiles table uses legalName as the company’s name field. However, some parts of the UI or code refer to a companyName. For example, the dashboard code uses assignment.clientProfile?.companyName to get a client’s name, which suggests an inconsistency (the API likely returns legalName or maps it to a field named company). We need to note these discrepancies for later fixes. There is also a ClientProfile page (for viewing/editing an individual client). We should verify what data it displays and how it expects that data (to address the edit form issues later).
Workers Page & Model: The Workers page lists employees/foreign workers associated with clients. The workers table in the DB contains extensive fields (personal info, contact, passport, employment details, etc.) and includes a clientProfileId to link each worker to a client. The Workers list likely shows basic info (name, maybe email or status). The WorkerProfile page shows detailed info on a selected worker. Notably, the current WorkerProfile is missing the workflow display or using placeholder data as mentioned. We should examine how the worker data is fetched (likely via an API call like /api/workers/:id) and what fields are returned. The worker data model on the front-end might have a fullName for convenience, and some computed fields like age might exist, but we must ensure consistency with backend responses.
Dashboard & Summary Data: The Dashboard provides an overview of the system (perhaps number of assignments, recent activity, etc.). It may aggregate data from clients, workers, and “assignments.” From the snippet, it appears the dashboard computes some client performance metrics by reducing over assignments, grouping by clientProfile.companyName. This implies the dashboard is using the legacy “assignments” data to show progress or counts per client. We’ll need to update this to use the new workflow tracking once we migrate that system (or at least to use consistent naming for client names). The dashboard may also display counts of open tasks, upcoming deadlines, etc., possibly using mock data if not fully wired up. We should list all such displayed info and verify whether each is coming from real API data or placeholders.
Workflow/Assignments: The app manages immigration workflows (AJOFM, IGI work permit, visa, etc.), which consist of multiple steps and document requirements. Currently, it seems to use a concept of “assignments” to track tasks in a workflow. Assignments have a status that can be NOT_STARTED, AWAITING_UPLOAD, SUBMITTED_BY_USER, etc., up to ACCEPTED/REJECTED. This is the legacy approach to workflow tracking. Additionally, there’s mention of a WorkflowAssignmentsPage in the routing, likely an admin view for assigning workflows to workers. Meanwhile, the new data model (from the database schema) introduces workflow templates and progress tracking tables:
workflow_templates & workflow_steps (defining the structure of each workflow and its steps),
worker_workflow_progress (one record per worker per assigned workflow, indicating overall status),
worker_step_progress (tracking each step’s status for that worker’s workflow).
We will need to reconcile the current functionality with this new model. Likely, the existing “assignments” correspond to individual step requirements, but they might not be fully using the new schema yet.
Forms and Edit Actions: Identify all forms (create/edit client, create/edit worker, etc.) and how they function currently. The prompt indicates that editing is broken due to state mismatches. For instance, when editing a client, the form might not populate correctly or the submission fails because the form’s field names don’t match what the API expects. We see in code that a new client form state uses keys like legalName, registrationNumber, etc.. We should confirm if the edit form reuses the same component/state or not. Similarly, check how editing a worker works. Inventory the form fields and their naming to ensure we later align them with the API schemas (e.g., if the API expects cui for the tax ID but the form uses a different name, that needs fixing).
Mock/Hardcoded Data: While scanning the code, note any obvious hardcoded values or placeholder logic. The prompt explicitly mentions hardcoded “industries” (perhaps a static list of industry options in a dropdown) and other dummy data. For example, the Reminders page currently uses a placeholder empty array instead of real data. Also, the DummyDataAlert component (visible in the layout) might indicate some parts of the UI are showing sample data until real data is connected. We should mark all these instances for replacement in the next steps.
By understanding the current state of clients, workers, dashboard, and workflow data models, we can plan the refactor to ensure each piece is handled. We see clear evidence of mismatches (like legalName vs companyName for client names) and incomplete implementations (workflow UI placeholders) which we will address systematically.